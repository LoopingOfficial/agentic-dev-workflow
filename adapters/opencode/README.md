# OpenCode adapter

This adapter installs custom command Markdown files under `.opencode/commands/`.

Each command delegates to the provider-neutral workflow and forwards `$ARGUMENTS` as optional task context.
