# Claude Code adapter

This adapter installs thin project command Markdown files under `.claude/commands/`.

Each command delegates to the provider-neutral workflow under `.agentic-workflow/workflows/`, keeping behavior aligned across providers.
