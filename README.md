# Agentic Dev Workflow

Open-source multi-agent development workflow for Codex, Claude Code, and OpenCode.

This repository is being initialized. The first public release will provide a shared, file-driven workflow for:

```text
audit → spec → build → review
```

with provider-specific adapters and reproducible validation.

MIT licensed.
